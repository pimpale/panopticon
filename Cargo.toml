[workspace]
resolver = "2"

members = [
    "panopticon",
    "panopticon-ics",
]

# Keep dependencies (image decoders in particular) optimized even in dev builds.
[profile.dev.package."*"]
opt-level = 3
