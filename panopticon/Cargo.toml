[package]
name = "panopticon"
version = "0.1.0"
edition="2024"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
chrono = "0.4.45"
clap = { version = "4.6.6", features = ["derive"] }
rand = "0.10.2"
xcap = "0.9.8"
user-idle = "0.6.0"
image = { version = "0.25", default-features = false, features = ["qoi"] }

